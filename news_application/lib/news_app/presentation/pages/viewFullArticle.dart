import 'package:flutter/material.dart';

class ViewFullArticle extends StatelessWidget {
  const ViewFullArticle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Full Article'),
      ),
      body: Container()
    );
  }
}
